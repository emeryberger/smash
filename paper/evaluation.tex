\section{Evaluation}
\label{sec:eval}

We structure the evaluation around five questions:

\begin{enumerate}
\item[\textbf{RQ1.}] How much does \sys reduce RSS on real
  applications, and what is the throughput and latency cost?
\item[\textbf{RQ2.}] How do compression algorithms compare on heap
  page data?
\item[\textbf{RQ3.}] What does each optimization contribute?
\item[\textbf{RQ4.}] How does allocator design affect page
  compressibility?
\item[\textbf{RQ5.}] How much of the benefit comes from the
  compression pipeline versus the allocator design?
\end{enumerate}

\subsection{Methodology}
\label{sec:eval:method}

We evaluate on Linux x86\_64 (Ubuntu~22.04, kernel~6.8,
glibc~2.35, 4\,KiB pages, 32\,GiB RAM, 8 cores; AWS m5.2xlarge).  \sys is loaded
via \tcode{LD\_\allowbreak PRELOAD}; RSS is sampled via
\tcode{/proc/\allowbreak[pid]/\allowbreak statm} at 1\,s intervals.
Each configuration is run three times; we report the median.

Benchmark applications: Redis~8.0.2 (built with libc malloc),
memcached~1.6.34, RocksDB~9.8.4, SQLite~3.51.0.
Compression libraries: LZ4~1.9.4, zstd~1.5.6.

\subsection{Application Benchmarks (RQ1)}
\label{sec:eval:apps}

We evaluate \sys's RSS reduction, throughput overhead, and
cold-access latency on four applications (six workloads) spanning
key-value stores, relational databases, and analytical engines.
Table~\ref{tab:apps_summary} summarizes the results.

\begin{table}[t]
\caption{\textbf{\sys reduces RSS by \rssRedMin--\rssRedMax\%}
  (\S\ref{sec:eval:apps}).  Serve-phase RSS (MiB) across allocators.
  Redis* = background tasks disabled; Redis-ext* = extended workload
  with 50\% key deletion; Redis$^\dagger$ = patched
  (metadata caching + idle mode), including DELETE.}
\label{tab:apps_summary}
\centering\small
\begin{tabular}{lrrr}
\toprule
Application & Sys & Mesh & \sys \\
\midrule
Memcached        & \ablMemcachedBaselineServeRss  & \ablMemcachedMeshServeRss  & \textbf{\ablMemcachedDefaultServeRss} (\ablMemcachedDefaultRssRedInt\%) \\
RocksDB          & \ablRocksdbBaselineServeRss  & \ablRocksdbMeshServeRss  & \textbf{\ablRocksdbDefaultServeRss} (\ablRocksdbDefaultRssRedInt\%) \\
SQLite           & \ablSqliteBaselineServeRss  & \ablSqliteMeshServeRss  & \textbf{\ablSqliteDefaultServeRss} (\ablSqliteDefaultRssRedInt\%) \\
Redis*           & \ablRedisBaselineServeRss  & \ablRedisMeshServeRss   & \textbf{\ablRedisDefaultServeRss} (\ablRedisDefaultRssRedInt\%) \\
Redis-ext*       & \ablRedisExtBaselineServeRss  & \ablRedisExtMeshServeRss   & \textbf{\ablRedisExtDefaultServeRss} (\ablRedisExtDefaultRssRedInt\%) \\
Redis$^\dagger$  & \ablRedisPatchedBaselineServeRss  & ---  & \textbf{\ablRedisPatchedDefaultServeRss} (\ablRedisPatchedDefaultRssRedInt\%) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/rss_reduction.pdf}
\caption{\textbf{RSS reduction scales with the fraction of cold data}
  (\S\ref{sec:eval:apps}).  Six workloads ordered by reduction.}
\label{fig:rss_reduction}
\end{figure}

The largest gains occur on workloads with clear hot/cold
partitions.  Figure~\ref{fig:rss_timeline} shows RSS over time for
all benchmarks.  Under \sys, RSS drops sharply during the cooling
phase as the compressor detects and compresses idle pages.  Baseline
RSS remains flat throughout.  Memcached shows \ablMemcachedDefaultRssRedInt\% reduction
(\ablMemcachedBaselineServeRss\,MiB to \ablMemcachedDefaultServeRss\,MiB);
RocksDB achieves \ablRocksdbDefaultRssRedInt\% reduction
(\ablRocksdbBaselineServeRss\,MiB to \ablRocksdbDefaultServeRss\,MiB);
SQLite achieves \ablSqliteDefaultRssRedInt\%
(\ablSqliteBaselineServeRss\,MiB to \ablSqliteDefaultServeRss\,MiB).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/rss_combined.pdf}
\caption{\textbf{\sys reduces RSS within seconds of data going
  cold} (\S\ref{sec:eval:apps}).  RSS over time for all workloads.
  Baseline (solid blue) stays flat; Mesh (dash-dot green) reduces RSS
  on some workloads via page meshing;
  \sys (dashed red) drops sharply during cooling.}
\label{fig:rss_timeline}
\end{figure*}

Figure~\ref{fig:auc} shows the \emph{area under the curve} (AUC) of
RSS over time---the integral of memory pressure across the benchmark.
Lower AUC means less total memory consumption.  \sys reduces AUC by
\aucRedMin--\aucRedMax\% across all workloads, where compression keeps
RSS low throughout the cooling and serving phases.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/auc_comparison.pdf}
\caption{\textbf{\sys reduces total memory pressure (AUC) by \aucRedMin--\aucRedMax\%}
  (\S\ref{sec:eval:apps}).
  AUC = integral of RSS over time; positive reduction = less memory pressure.}
\label{fig:auc}
\end{figure}

Figure~\ref{fig:cdf_cold} shows the CDF of cold-access latency
(the time to access a previously compressed page).  Under the system
allocator, cold accesses complete in under 1\,$\mu$s (no
decompression).  Under \sys, the median cold access is under
1\,$\mu$s, with a tail from zstd decompression of large~objects.

\begin{figure}[t]
\centering
\includegraphics[width=0.81\columnwidth]{figures/cdf_cold_combined.pdf}
\caption{\textbf{Cold-access latency is under 10\,$\mu$s at the
  median for all workloads} (\S\ref{sec:eval:apps}).  Tail reflects
  zstd decompression of large objects.}
\label{fig:cdf_cold}
\end{figure}

\paragraph{Memcached}
We load 500K TPC-H JSON records into memcached, then serve a hot 5\%
for 30 seconds before re-accessing cold keys.  Serve-phase
RSS drops from \ablMemcachedBaselineServeRss\,MiB to \ablMemcachedDefaultServeRss\,MiB (\ablMemcachedDefaultRssRedInt\%).  Cold-key re-access
benefits from \sys prefetching adjacent pages on fault.  Throughput
is unchanged.

Fill-phase RSS is $\sim$8\% higher due to the
bootstrap allocator, page state arrays, and compression contexts.
This fixed overhead is amortized as the working set~grows.

\paragraph{SQLite}
An in-memory database with 1M records queried with random lookups.
RSS drops from \ablSqliteBaselineServeRss\,MiB to \ablSqliteDefaultServeRss\,MiB (\ablSqliteDefaultRssRedInt\% reduction).
Query throughput is \ablSqliteDefaultOps~ops/s, comparable to the~baseline.

\paragraph{RocksDB}
A 256\,MiB uncompressed block cache populated with 200K key-value
pairs.  Serve-phase RSS drops from \ablRocksdbBaselineServeRss\,MiB to \ablRocksdbDefaultServeRss\,MiB (\ablRocksdbDefaultRssRedInt\%).
RocksDB's built-in compression (LZ4 or zstd) reduces
data size on disk but not in the block cache, since blocks are
decompressed on load.  \sys and RocksDB compression are
complementary: they operate at different levels of the
storage~hierarchy.

\paragraph{Redis}
We evaluate two workloads with \texttt{redis-bench\-mark}
(100K ops, 1000\,B values, 5K connections, LLAMA
config~\cite{maas2020adaptive}).
The \emph{standard} workload fills the keyspace and measures
post-cooling RSS\@.  The \emph{extended} workload deletes
50\% of keys before cooling, creating fragmentation
that lets allocators exploiting partially-filled pages
reclaim space.

Redis's event loop and background tasks (active defragmentation,
incremental rehashing, lazy-free) can keep pages warm even during
idle periods.  To achieve effective compression, we configure Redis
with minimal background activity
(\tcode{hz\,1}, \tcode{activedefrag\,no}, etc.).
Disabling these tasks does not affect baseline performance: jemalloc
achieves identical RSS and throughput with or without background tasks.
With this configuration, \sys achieves \ablRedisDefaultRssRedInt\% RSS reduction
(\ablRedisBaselineServeRss\,MiB to \ablRedisDefaultServeRss\,MiB) and
\aucRedisSmashRed\% lower AUC (\ablRedisDefaultAuc\ vs.\ \ablRedisBaselineAuc\,MB$\cdot$s).

On the extended workload (SET $\to$ DELETE~50\% $\to$ cool $\to$ GET),
\sys achieves \ablRedisExtDefaultRssRedInt\% RSS reduction
(\ablRedisExtBaselineServeRss\,MiB to \ablRedisExtDefaultServeRss\,MiB).
Throughput is comparable across all configurations ($\pm$5\%).

\paragraph{Root cause analysis and Redis patches}
Two specific Redis behaviors prevent effective compression.
First, \tcode{server\-Cron()}'s background
operations---active defragmentation, incremental rehashing, and hash
table resizing---traverse key data pages every cron tick, keeping them
warm even when no client commands are being processed.  With
\tcode{-{}-hz\,1} and all configurable background tasks disabled,
only 0.4\% of pages reach the cold threshold because
\tcode{server\-Cron} still runs defrag and rehash unconditionally.

Second, \tcode{DEL} calls \tcode{get\-Object\-Length()} to
update the keysizes histogram.  This dereferences the value pointer
into value data pages (SDS headers, dict sizes, etc.),
triggering SIGSEGV on compressed pages and causing wholesale
decompression.  Similarly, the async-delete path calls
\tcode{kvobj\-Alloc\-Size()} for memory tracking, which traverses
value data structures.

We address both issues with minimal, semantics-preserving
patches to Redis (7~files, 105~lines added, 51~removed).
For the \tcode{DEL} problem, we add two write-through cache
fields to the \tcode{kvobj} header: \tcode{cached\_\allowbreak length} and
\tcode{cached\_\allowbreak alloc\_\allowbreak size}.  Both are updated transparently
on every call to \tcode{get\-Object\-Length()} and
\tcode{kvobj\-Alloc\-Size()} respectively, so all 58+~call sites
in Redis command handlers keep the cache current with no
modifications.  The delete path reads these cached values from
the object header (same cache line as \tcode{type} and
\tcode{encoding}) instead of dereferencing into value data pages.
Keysizes histograms and slot allocation tracking are preserved.

For the \tcode{server\-Cron} problem, we add an
\tcode{idle-mode} configuration option.  When enabled, Redis
tracks whether any commands have been processed in the last three
cron ticks.  During idle periods, \tcode{databases\-Cron()} skips
only the three operations that inherently traverse key data
pages: active defragmentation, incremental rehashing, and
dictionary resizing.  All other operations
continue unconditionally: active key expiration (TTL correctness),
client timeout enforcement, allocator statistics, replication
and cluster heartbeats, AOF persistence, and all periodic cleanup
tasks.  When client commands resume, the idle counter resets and
all operations return to normal.

Table~\ref{tab:redis_patched} compares stock and patched Redis.
The patches lower the \emph{baseline} RSS from
\ablRedisBaselineServeRss\,MiB to \ablRedisPatchedBaselineServeRss\,MiB
(a \redisPatchBaselineRedPct\% reduction even without \sys),
because idle-mode prevents background tasks from inflating RSS\@.
With \sys, patched Redis achieves \ablRedisPatchedDefaultRssRedInt\%
RSS reduction (\ablRedisPatchedBaselineServeRss\,MiB to
\ablRedisPatchedDefaultServeRss\,MiB).  While the percentage
reduction is smaller than stock Redis (\ablRedisDefaultRssRedInt\%),
this is because the baseline is already lower---the absolute
minimum RSS values are comparable
(\ablRedisPatchedDefaultMinRss\,MiB patched vs.\
\ablRedisDefaultMinRss\,MiB stock).

On the extended workload (with 50\% DELETE), patched Redis achieves
\ablRedisExtPatchedDefaultRssRedInt\% reduction
(\ablRedisExtPatchedBaselineServeRss\,MiB to
\ablRedisExtPatchedDefaultServeRss\,MiB), compared to
\ablRedisExtDefaultRssRedInt\% for stock Redis.  Critically,
patched Redis achieves comparable reduction on
\emph{both} workloads (\ablRedisPatchedDefaultRssRedInt\% standard
vs.\ \ablRedisExtPatchedDefaultRssRedInt\% extended), whereas stock
Redis drops from \ablRedisDefaultRssRedInt\% to
\ablRedisExtDefaultRssRedInt\% after DELETE\@.

\begin{table}[t]
\caption{\textbf{Patched Redis lowers the baseline; \sys compresses
  the remainder}
  (\S\ref{sec:eval:apps}).  Serve-phase RSS (MiB) for stock vs.\
  patched Redis (with idle-mode and metadata caching).}
\label{tab:redis_patched}
\centering\small
\begin{tabular}{lrrrr}
\toprule
 & \multicolumn{2}{c}{Standard} & \multicolumn{2}{c}{Extended (DEL 50\%)} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
Config & Baseline & \sys & Baseline & \sys \\
\midrule
Stock Redis   & \ablRedisBaselineServeRss  & \textbf{\ablRedisDefaultServeRss} (\ablRedisDefaultRssRedInt\%) & \ablRedisExtBaselineServeRss  & \textbf{\ablRedisExtDefaultServeRss} (\ablRedisExtDefaultRssRedInt\%) \\
Patched Redis & \ablRedisPatchedBaselineServeRss & \textbf{\ablRedisPatchedDefaultServeRss} (\ablRedisPatchedDefaultRssRedInt\%) & \ablRedisExtPatchedBaselineServeRss & \textbf{\ablRedisExtPatchedDefaultServeRss} (\ablRedisExtPatchedDefaultRssRedInt\%) \\
\bottomrule
\end{tabular}
\end{table}

The patches illustrate a general principle: applications that
access heap data structures during idle periods interfere with
page-level compression.  Separating metadata from data---so that
bookkeeping operations read cached values in object headers rather
than dereferencing into data pages---and deferring
data-page-touching maintenance until the application is active
are sufficient to make such applications compatible with
transparent heap compression.

\paragraph{Comparison across allocators}
To confirm that compression, not allocator choice, drives the
RSS reduction, we run all benchmarks with
Mesh~\cite{powers2019mesh} as a representative alternative allocator
(Table~\ref{tab:apps_summary}).  Mesh uses external metadata and virtual-memory
remapping to merge two virtual pages onto one physical page when
their live objects do not overlap.
Mesh provides minimal benefit on most workloads
(matching or exceeding baseline RSS on memcached and SQLite).
\sys achieves \rssRedMin--\rssRedMax\%
reduction on these workloads via compression.
Without a compression pipeline, even Mesh's aggressive page
remapping cannot reclaim space from pages with live~objects.

\punt{
\paragraph{Mesh-paper Redis workload}
Powers et al.~\cite{powers2019mesh} report that Mesh reduces Redis
RSS by 39\% on a workload designed to induce fragmentation: 700K
keys with 240-byte values followed by 170K keys with 492-byte
values, under a 100\,MiB LRU cap.  We replicate this workload
(Table~\ref{tab:redis_mesh}).  No allocator---including Mesh,
jemalloc's activedefrag, and \sys---reduces RSS.  All
configurations show a $\sim$1.9$\times$ fragmentation ratio.

\begin{table}[t]
\caption{\textbf{Mesh's Redis fragmentation result does not
  reproduce on modern systems} (\S\ref{sec:eval:apps}).
  RSS (MiB) on the Mesh-paper Redis workload
  (700K$\times$240\,B + 170K$\times$492\,B, maxmemory=100\,MiB LRU).
  Frag = fragmentation ratio (RSS / used).}
\label{tab:redis_mesh}
\centering\small
\begin{tabular}{lrrr}
\toprule
Configuration & Final RSS & Reduction & Frag \\
\midrule
System malloc            & 192  & ---    & 1.92 \\
jemalloc                 & 190  & ---    & 1.90 \\
jemalloc + activedefrag  & 191  & 0\%    & 1.91 \\
Mesh                     & 194  & $-$1\% & 1.94 \\
\sys                     & 207  & $-$4\% & 2.07 \\
\bottomrule
\end{tabular}
\end{table}

The root cause is that the fragmentation Mesh exploited no longer
occurs.  The original experiment used Redis~4.0.2 with
jemalloc~3.6.0; we use Redis~8.0.2 with jemalloc~5.3.0.  Modern
jemalloc handles size-class transitions (240\,B $\to$ 492\,B) with
far less fragmentation, and Redis itself has improved its internal
memory management over five major versions.  The result is that LRU
eviction immediately replaces freed objects with new insertions,
keeping pages fully occupied---there are no partially-filled pages
for Mesh to compact and no cold pages for \sys to compress.  The
$\sim$1.9$\times$ ratio reflects fixed per-object metadata overhead
(SDS headers, dict entries, alignment padding), not reclaimable
holes.  \sys's own metadata (page-state tracking, bootstrap
allocator) adds $\sim$15\,MiB, slightly increasing RSS on this
fully-active dataset.
}

\paragraph{Summary}
\sys reduces RSS by \rssRedMin--\rssRedMax\% on workloads where data stays cold
(memcached, RocksDB, SQLite, Redis with background tasks disabled),
with negligible throughput cost and sub-10\,$\mu$s median
cold-access latency.
The benefit scales with the fraction of cold data and data
compressibility.  Compression, not allocator choice,
drives the savings: Mesh provides minimal benefit on most workloads,
while \sys achieves consistent reductions via compression.
Two small Redis patches---caching object metadata in headers and
deferring data-page-touching maintenance during idle
periods---lower the baseline RSS and enable consistent compression
across both standard and extended workloads
(Table~\ref{tab:redis_patched}).

\subsection{Algorithm Comparison (RQ2)}
\label{sec:eval:algo}

To understand which compression algorithms are best suited to heap
page data, we compare LZ4 and zstd at several levels on
pages captured from the four application heaps.
Figure~\ref{fig:algo_compare} presents compression ratios and
Table~\ref{tab:algo_throughput} presents throughput.  \punt{WKdm, the macOS VM compressor, achieves only 1.0--1.1$\times$
on text data because its 32-bit word-granularity dictionary cannot
match byte-level patterns.  It performs adequately on binary data
(6.4$\times$ on SQLite pages).  }LZ4 achieves 4.7--12.3$\times$ compression at
$\sim$3\,GB/s.  zstd-1 achieves 1.5--2$\times$ higher ratios
(8.8--20.4$\times$) at $\sim$2$\times$ lower throughput, because its
entropy coder (FSE) captures byte-frequency skew that LZ4's
literal-copy format cannot exploit.  zstd-9 achieves the best
ratios (9--21$\times$) at lower~throughput.

\begin{table}[t]
\caption{\textbf{zstd-1 trades throughput for better ratios; zstd-9
  decompresses as fast as zstd-1}
  (\S\ref{sec:eval:algo}).  Throughput in MB/s on SQLite heap pages.}
\label{tab:algo_throughput}
\centering\small
\begin{tabular}{lrr}
\toprule
Algorithm & Compression & Decompression \\
\midrule
LZ4    & \textbf{2955} & \textbf{4184} \\
zstd-1 & 1397 & 1797 \\
zstd-3 & 1252 & 1768 \\
zstd-9 & 731  & 1823 \\
\bottomrule
\end{tabular}
\punt{%% Full table with JSON and WKdm:
\begin{tabular}{lrrrr}
\toprule
 & \multicolumn{2}{c}{Compression} & \multicolumn{2}{c}{Decompression} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
Algorithm & JSON & SQLite & JSON & SQLite \\
\midrule
WKdm   & 1865 & 1819 & 10077 & 5269 \\
LZ4    & 1004 & 2955 & 6064  & 4184 \\
zstd-3 & 553  & 1252 & 1498  & 1768 \\
zstd-9 & 90   & 731  & 1437  & 1823 \\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/algo_compare.pdf}
\caption{\textbf{zstd-1 achieves 1.5--2$\times$ higher ratios than
  LZ4 at comparable throughput; zstd-9 adds a further 1.0--1.3$\times$}
  (\S\ref{sec:eval:algo}).  Higher is better.}
\label{fig:algo_compare}
\end{figure}

\paragraph{Summary}
Heap pages compress well: zstd-1 achieves 8.8--20.4$\times$ compression
at 0.6--1.4\,GB/s, and zstd-9 reaches 9--21$\times$ on structured data at
lower throughput.  zstd-1's entropy coder gives 1.5--2$\times$ better
ratios than LZ4 at $\sim$2$\times$ lower throughput; for cold pages
that remain idle, the better ratio yields higher net~ROI\@.
These results motivate \sys's ROI-based JIT
compression: because highly compressible data is also \emph{cheaper}
to compress (higher throughput), the ROI is doubly favorable---the
model naturally selects zstd-1 for recently cold pages (where fast
compression minimizes risk) and zstd-9 for long-idle pages (where the
better ratio justifies the~cost).

\subsection{Ablation Study (RQ3)}
\label{sec:eval:ablation}

To quantify what each optimization contributes, we selectively
disable individual features and measure the change in RSS reduction.
We run each configuration across all four applications (six workloads),
each with 3 runs.  Ablation requires rebuilding \sys with different
compile-time constants; external-process benchmarks (memcached, Redis)
are run via library interposition (\texttt{LD\_PRELOAD}) against each
rebuilt library.
Figure~\ref{fig:ablation} shows the~results.

The most striking result is that enabling dictionaries
\emph{reduces} RSS savings (\ablAvgDictDelta\,\pp average) across all benchmarks.
CDict objects consume 686\,KiB each at level~9, and at that
compression level, zstd's search window already captures the
patterns a dictionary would provide.  \sys therefore disables
dictionaries by default.

Disabling compression entirely (\ablAvgNoCompressDelta\,\pp average) confirms that
compression is the dominant mechanism; without it, \sys reduces to
a standard allocator with no RSS benefit beyond what the allocator
design alone provides.  The remaining optimizations---arenas, fast-only
mode (disabling zstd-9 re-compression), deferred zeroing, prefetch, and
parallelism---each contribute modestly individually.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/ablation.pdf}
\caption{\textbf{Dictionaries and disabling compression are the
  only configurations with large impact}
  (\S\ref{sec:eval:ablation}).  Change in RSS reduction (\%)
  from default.}
\label{fig:ablation}
\end{figure}

\paragraph{Summary}
Compression is the dominant mechanism: disabling it eliminates
virtually all RSS savings (\ablAvgNoCompressDelta\,\pp).  Dictionaries are
actively harmful (\ablAvgDictDelta\,\pp average) due to CDict memory
overhead.  The remaining optimizations each contribute modestly
individually, confirming that \sys's default configuration
is close to~optimal.

\subsection{Allocator Design Impact (RQ4)}
\label{sec:eval:allocator}

The previous experiments measure \sys end-to-end.  We now ask how
allocator design alone affects page compressibility, independent of
any compression pipeline.  For each of nine allocators, we run the
same application workloads used in RQ1---SQLite (50K rows,
no frees), memcached-format slab items, and Redis SDS strings
(100K objects each, 50\% freed in an
alternating pattern)---and compress the resulting heap pages with
zstd-9.

Table~\ref{tab:allocator_features} classifies each allocator by three
design properties relevant to compression: whether metadata (freelists,
bitmaps) is stored outside data pages, whether freed slots are
zeroed, and whether allocations are segregated by call site.

\begin{table}[t]
\caption{\textbf{Only \sys combines all three design properties
  that improve compressibility} (\S\ref{sec:eval:allocator}).
  \ding{51} = feature present.}
\label{tab:allocator_features}
\centering\small
\begin{tabular}{lccc}
\toprule
Allocator & External & Zero-on- & Arena \\
          & metadata & free     & segregation \\
\midrule
System malloc &  &  &  \\
mimalloc      &  &  &  \\
jemalloc      &  &  &  \\
tcmalloc      &  &  &  \\
Hoard         &  &  &  \\
Mesh          & \ding{51} &  &  \\
DieHard       & \ding{51} &  &  \\
DieHarder     & \ding{51} & \ding{51} &  \\
\sys          & \ding{51} & \ding{51} & \ding{51} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/allocator_compare.pdf}
\caption{\textbf{Zero-on-free is the single most important
  allocator design choice for compressibility}
  (\S\ref{sec:eval:allocator}).  Compression ratio =
  original\,/\,compressed (higher is better) with zstd-9 on
  real application heap pages.
  \dag~marks allocators that zero freed memory:
  DieHarder (security zeroing)
  and Smash (deferred to compression time---not yet applied in
  this static measurement).}
\label{fig:allocator_compare}
\end{figure*}

Figure~\ref{fig:allocator_compare} presents the compression ratios.
The results yield two observations.

First, zero-on-free is the dominant factor for page compressibility,
and its effect is workload-dependent.  Adding explicit zeroing before
\texttt{free} improves every allocator, confirming the effect is
causal.

Second, when frees are absent, allocator choice barely matters.
All allocators achieve similar compression because every byte on the
page is live, structured data.

\paragraph{Summary}
Zero-on-free is the single most important allocator design choice
for compressibility, with dramatic effects on data with low
byte-level redundancy and modest effects on text-heavy data.
When no frees occur, allocator choice is irrelevant.
These results motivate \sys's deferred zero-on-free: it captures the
compressibility benefit without adding latency to the
\texttt{free}~path.

\subsection{Pipeline vs.\ Allocator Design (RQ5)}
\label{sec:eval:pipeline}

Finally, we ask how much of \sys's benefit comes from the
compression pipeline versus its allocator design choices.
We decompose the contribution via two experiments.

\paragraph{Compress-only across allocator substrates}
We build a \emph{compress-only}
library
that adds \sys's compression pipeline \emph{on top of} an
unmodified allocator.  This library interposes
\texttt{malloc}/\texttt{free} solely to discover which pages
belong to the heap; all allocation requests are forwarded to
the underlying allocator unchanged.  It then applies \sys's page monitoring,
JIT compression (zstd-1$\to$zstd-9 tiering), signal-based
decompression, and prefetching to those pages.  Because allocation is still performed
by the underlying allocator, pages contain embedded freelists, objects are
not zeroed, and allocations are not segregated by call site.
On Linux, the compress-only library discovers heap regions by
parsing \texttt{/proc/self/maps}.

We test compress-only on three allocator substrates: glibc
system malloc, jemalloc~5.3.0, and mimalloc~3.3.0 (both built
from source).  We also build \emph{\sys-noopt}, a variant that
uses \sys's allocator but disables the allocation-design
optimizations (arenas${}={}$1, no zero-on-free), isolating the
contribution of owning the allocation from the optimizations
themselves.

\begin{table}[t]
\caption{\textbf{Replacing the allocator is necessary for compression;
  optimizations are not.}  Compress-only adds \sys's pipeline atop
  the given allocator; \sys-noopt uses \sys's allocator with 1~arena
  and no zero-on-free (\S\ref{sec:eval:pipeline}).}
\label{tab:compress_only}
\centering\footnotesize
\begin{tabular}{llrr}
\toprule
Benchmark & Config & Min RSS & Reduction \\
\midrule
\multirow{5}{*}{Memcached}
  & system malloc           & \vaMemcachedBaselineRss  & ---  \\
  & jemalloc                & \vaMemcachedJemallocRss  & 0\% \\
  & mimalloc                & \vaMemcachedMimallocRss  & 0\% \\
  & sys.\ + compress-only   & \vaMemcachedSysCompRss   & $-$\vaMemcachedSysCompOh\% \\
  & je.\ + compress-only    & \vaMemcachedJeCompRss    & $-$\vaMemcachedJeCompOh\% \\
  & mi.\ + compress-only    & \vaMemcachedMiCompRss    & $-$\vaMemcachedMiCompOh\% \\
  & \sys-noopt              & \textbf{\vaMemcachedNooptRss} & \textbf{\vaMemcachedNooptRed\%} \\
  & full \sys               & \textbf{\vaMemcachedFullRss}   & \textbf{\vaMemcachedFullRed\%} \\
\midrule
\multirow{4}{*}{SQLite}
  & system malloc           & \vaSqliteBaselineRss     & ---  \\
  & sys.\ + compress-only   & \vaSqliteSysCompRss      & $-$\vaSqliteSysCompOh\% \\
  & mi.\ + compress-only    & \vaSqliteMiCompRss       & $-$\vaSqliteMiCompOh\% \\
  & \sys-noopt              & \textbf{\vaSqliteNooptRss} & \textbf{\vaSqliteNooptRed\%} \\
  & full \sys               & \textbf{\vaSqliteFullRss}  & \textbf{\vaSqliteFullRed\%} \\
\midrule
\multirow{3}{*}{RocksDB}
  & baseline       & \coRocksdbBaselineRss  & ---  \\
  & compress-only  & \coRocksdbCompOnlyRss  & $-$\coRocksdbCompOnlyOverhead\% \\
  & full \sys      & \textbf{\coRocksdbFullSmashRss}   & \textbf{\coRocksdbFullSmashReduction\%} \\
\midrule
\multirow{3}{*}{Redis}
  & baseline       & \coRedisBaselineRss   & ---  \\
  & compress-only  & \coRedisCompOnlyRss  & $-$\coRedisCompOnlyOverhead\% \\
  & full \sys      & \textbf{\coRedisFullSmashRss}    & \textbf{\coRedisFullSmashReduction\%} \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:compress_only} presents the results.
Compress-only is ineffective on \emph{every} allocator substrate:
it increases RSS by \vaSqliteSysCompOh--\vaMemcachedSysCompOh\%
due to metadata overhead (VM region, page-state arrays, compression
contexts) without corresponding compression~benefit.  The result
is the same whether the underlying allocator is system malloc,
jemalloc, or mimalloc: pages managed by a third-party allocator are
never sufficiently cold to compress.

The critical factor is \emph{owning the allocation}.
\sys-noopt---which uses \sys's allocator but disables arenas and
zero-on-free---achieves \vaMemcachedNooptRed\% reduction on
memcached and \vaSqliteNooptRed\% on SQLite, matching full \sys
within noise.
The essential property is that \sys's allocator places objects in
its own VM~region where the compressor has full control over page
protection and can decompress transparently on fault.
Third-party allocators use their own VM management (Mach zones on
macOS, \texttt{mmap} arenas on Linux), and external page scanning
cannot reliably track access patterns within those regions.

Full \sys achieves
\coRedisPatchedFullSmashReduction--\vaMemcachedFullRed\%
reduction.  The allocator design contributes most on RocksDB
(\coRocksdbFullSmashReduction\% reduction),
where block-cache entries span entire pages and
embedded freelist pointers from the system allocator degrade
compression.

\paragraph{Why compress-only fails on memcached and Redis}
On memcached, the slab allocator manages items within 1\,MiB
\texttt{malloc}'d chunks; even with the LRU crawler disabled,
internal slab bookkeeping keeps pages warm.
On Redis, the event loop touches heap pages even at minimum
frequency (\texttt{hz\,1}), preventing the pipeline's VM-region
scanner from finding cold pages.
Full \sys avoids both problems because it tracks page access
internally at the allocator level.  With all background tasks
disabled, full \sys achieves \coRedisFullSmashReduction\% RSS
reduction and \aucRedisSmashRed\% lower AUC on Redis;
disabling these tasks does not affect the jemalloc baseline.
With the metadata-caching and idle-mode patches
described in \S\ref{sec:eval:apps}, the patched Redis baseline
is already \ablRedisPatchedBaselineServeRss\,MiB (vs.\
\ablRedisBaselineServeRss\,MiB stock), and \sys achieves
\ablRedisPatchedDefaultRssRedInt\% further reduction because the
patches eliminate data-page warming from background tasks
and \tcode{DEL}.

\paragraph{Summary}
The critical value-add is \emph{allocator ownership}, not
allocation-design optimizations.
\sys-noopt (one arena, no zeroing) matches full \sys because the
essential property is placing objects in a region where the
compressor controls page protection and can zero freed slots at
compression time.
Compress-only adds overhead rather than reducing RSS on every
allocator substrate tested (system malloc, jemalloc~5.3.0,
mimalloc~3.3.0), because third-party allocators' internal page
management prevents the compression pipeline from discovering
cold pages.
The pipeline and allocator are complementary:
the pipeline provides the compression infrastructure, while
allocator ownership provides the page-level control that makes
compression effective.
