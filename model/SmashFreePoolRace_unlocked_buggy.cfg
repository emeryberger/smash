\* "unlocked" = PRE-FIX: newFreeRun (decommit thread) pops free_pool WITHOUT the
\* shard lock, racing recycleFreeRun's locked push. The pop can fire during the
\* push's non-atomic RMW window → NoCorrupt VIOLATED (the data race that makes
\* free_pool cyclic → LargeAlloc/decommit spin-livelock).
\* See SmashFreePoolRace.lean : unlocked_violates_safe.
CONSTANTS
    Variant = "unlocked"
SPECIFICATION Spec
INVARIANT NoCorrupt
INVARIANT TypeOK
