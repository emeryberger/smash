\* SmashCoreSym — FIXED mode (BuggyMode = FALSE). Expect: no violation.
\* Symmetry-reduced: Pages / AppThreads / Compressors are model-value sets.
CONSTANTS
    Pages         = {p1, p2, p3}
    AppThreads    = {t1, t2}
    Compressors   = {c1, c2}
    ColdThreshold = 1
    MaxReleases   = 1
    BuggyMode     = FALSE

SPECIFICATION Spec
SYMMETRY AllSymmetry
INVARIANT TypeOK
INVARIANT SafetyInv
