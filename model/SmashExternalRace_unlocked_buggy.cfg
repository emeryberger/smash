\* "unlocked" external munmap = the PRE-FIX code: deregisterLinuxExternalRange
\* sets PageState=EMPTY + untracks + real_munmap WITHOUT taking the per-page
\* lock. NoUnmappedRead is VIOLATED: a worker that locked the page and marked it
\* COMPRESSING then snapshots it after munmap has torn down the mapping.
\* (See SmashExternalRace.lean : unlocked_violates_safe.)
CONSTANTS
    Variant = "unlocked"

SPECIFICATION Spec
INVARIANT NoUnmappedRead
INVARIANT TypeOK
